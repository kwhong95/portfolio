import { css } from '@emotion/react';

export const GlobalStyle = css`
  html {
    font-family: 'Noto Sans KR', sans-serif;
    font-size: 14px;
  }

  body {
    overflow-x: hidden;
    letter-spacing: -.05em;
    background: #101010;
    color: #fff;
  }

  p {
    line-height: 1.6;
  }
`;